<project name="common" basedir=".">

  <dirname property="common.basedir" file="${ant.file.common}"/>

  <property name="buildbase.dir" value="${common.basedir}/tools/buildbase"/>
	
  <fail>>

Please, prepare build environment:
1. run 'ant -f ..${file.separator}setup.xml' to get latest BuildBase build script.
2. run Ant again
    <condition>
      <not>
        <available file="${buildbase.dir}/buildbase.xml"/>
      </not>
    </condition>
  </fail>

  <import file="${buildbase.dir}/buildbase.xml"/>

</project>
